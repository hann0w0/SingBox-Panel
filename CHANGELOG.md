# SingBox Panel v1.0.2

面板与 Agent 版本统一为 **v1.0.2**。本次发布集中修复配置下发、节点状态与备份恢复的可靠性问题，并重做登录页。数据库无需迁移，现有配置与数据保持不变。

## 界面更新

- 登录页改为纯白底、登录卡片居中，主按钮配色与面板强调色统一（不再使用独立的深灰品牌色）。
- 退出登录会在服务端撤销当前会话，已登出或泄露的令牌立即失效。
- 修复日志页刷新历史日志时，恰好到达的实时日志行被覆盖丢失的问题。
- 编辑使用内嵌证书的 TLS 入站时不再清空证书内容，只有填写证书路径时才切换为路径方式，避免保存后节点配置失效。
- 编辑出站时保留原有的 Host 请求头（含重复或自定义 Host），修复紧凑表单未展示该字段时被清空的问题。

## 修复与调整

- 写入节点的 `/etc/sing-box/config.json` 改为缩进的多行 JSON，便于通过 SSH / SFTP 直接查看；格式化在哈希与校验之前完成，重复下发仍判定为「配置未变」，不会因此重启 sing-box。
- 下发配置不再擅自把被手动禁用的 sing-box 设为开机自启：服务未运行时只启动，不改动开机自启状态。
- 修复节点配置下发在等待同一节点上一次操作时被超时中断的问题；后台下发失败会自动重试，减少「已保存但没生效」的情况。
- 修复节点重连瞬间被误判为离线，以及被替换的旧连接的过期状态、流量与心跳覆盖新连接数据的问题。
- 修复节点流量增量在事件发送失败时被提前清空、导致漏计的问题，断线或面板繁忙时不再丢失累计流量。
- 节点流量统计改由数据库聚合，长时间范围的图表不再一次性加载全部记录而卡顿或超时。
- Agent 自更新或退出时立即关闭与面板的连接，不再等待约 60 秒超时，更新结果确认更快。
- Agent 更新只有在新版本真正启动并连上面板（校验值匹配）后才判定成功，否则自动回滚，避免显示成功却仍在运行旧版本。
- 修复节点卸载 sing-box 失败后残留半卸载状态的问题：失败会自动恢复配置与服务状态，服务未真正停止时拒绝继续。
- 修复节点日志跟随在取消或遇到超长日志行后可能残留后台日志进程的问题。
- 修复恢复备份过程中断电或进程被终止，导致数据库与登录密钥不一致、面板无法启动的问题：启动时依据恢复事务记录自动补完或回退，无法判断的状态会明确报错而不是猜测处理。
- 恢复备份时若数据库文件已被外部删除（改名或文件系统层面丢失），不再因为读不到文件而直接拒绝启动：回滚快照校验通过时会用它把数据库恢复回来，快照缺失或损坏才报错并要求人工处理。数据库路径是指向其他挂载的符号链接、而链接目标暂时不可用时，会拒绝自动恢复并明确提示，避免把符号链接替换成普通文件、在挂载恢复后与真实数据库静默分叉。
- 修复非 SQLite（MySQL / PostgreSQL）部署上数据库迁移中途失败后被永久标记为中断、面板再也无法启动的问题：仅对可安全重放的迁移自动重试，其余仍要求先恢复迁移前备份。
- 修复早期版本数据库升级时节点授权字段迁移失败的问题，以及迁移前备份清理失败导致升级中断的问题。
- 首次启动若未提供管理员密码，面板不再自动生成随机密码并写入服务日志，而是明确报错要求显式设置。
- 修复导入分享链接时遇到无法识别的传输方式会静默生成缺参数节点的问题，现在会明确提示；同时修复部分 ss:// 链接与 ALPN 列表解析失败。
- 修复节点名称或凭据中的换行、控制字符可能破坏或注入 Surge 订阅内容的问题，此类节点会被跳过并提示。
- 限制批量操作一次提交的 ID 数量，超限会明确提示，避免异常请求拖慢面板。
- 删除仍有连接在传输数据的节点时，会提示稍后重试而不是留下不完整数据；节点状态刷新也不会再写入已被替换连接的过期结果。
- 节点流量统计使用的本地端口不再允许被选作入站监听端口，创建时会明确提示。
- 修复系统时间回退或备份时间戳异常后，OneDrive 自动备份长期不再执行的问题。
- 面板导入备份时，同版本（含自编译版本）不再被误判为版本不兼容。
- 面板安装与更新时的下载失败会自动重试，网络抖动不再直接中断安装。
- 卸载面板时若服务或面板进程仍在运行会明确拒绝并终止，避免删除文件时留下运行中的进程和残留数据。
- 重装或重新配置面板时按原样沿用已保存的访问域名，不再因重复校验旧配置而中断安装；安装失败回滚时会一并撤销本次安装写入的开机自启设置。
- Agent 安装脚本增加服务加固（不提升权限、独立临时目录、隐藏家目录、更严格的默认文件权限），下载同样支持失败重试。

## 升级提示

**MySQL / PostgreSQL 部署必须在升级前显式设置 `jwt_secret`**：此前空的 `jwt_secret` 会被静默接受并临时生成，v1.0.2 起非 SQLite 部署缺少显式密钥会直接拒绝启动。SQLite 部署无需任何操作（密钥自动落盘保存），但请不要更换它——更换会让现有登录会话立即失效，且已加密保存的 OneDrive 授权将无法解密，需要重新授权。

面板更新会同步更新可分发的 Agent 包，**但已连接服务器上的 Agent 需在面板「服务器」页手动更新**。本次 Agent 侧的可靠性修复（配置文件多行格式、服务启停语义、自更新校验、卸载安全、日志跟随、状态校验）都要更新后才生效。节点 Agent 更新后第一次下发配置会把现有配置重写为多行格式（内容不变），因此这一次会重启 sing-box、造成短暂断连，之后重复下发不再重启。建议逐台更新，避开业务高峰。

已安装 v1.0.1 的用户可在面板更新区域直接更新到 v1.0.2，或再次运行下方安装命令。

## 安装 / 更新

```bash
curl -fsSL https://github.com/hann0w0/SingBox-Panel/releases/download/v1.0.2/install.sh | sudo bash -s -- --version v1.0.2
```

采用原生二进制 + systemd 部署。通常重跑安装会保留现有配置与数据。完整说明见 [README](https://github.com/hann0w0/SingBox-Panel/blob/v1.0.2/README.md)。

提供 Linux amd64 / arm64 面板、双架构 Agent 包、前端资源、安装脚本及 SHA-256 校验和。构建产物附带 GitHub Artifact Attestation。

---

# SingBox Panel v1.0.1

面板与 Agent 版本统一为 **v1.0.1**。本次重新发布整合原 v1.0.1 的问题修复和新版登录页、管理员及用户界面。

## 界面更新

- 更新登录页品牌 Logo、标题配色与浏览器图标。
- 桌面登录页采用 6:4 分栏，三行品牌文字加入渐隐渐现动效；手机端保留简洁登录表单。
- 管理员与用户界面统一提供日间、夜间及跟随系统的自动主题，支持浏览器记忆与跨标签页同步。
- 调整导航、卡片、表格、表单与流量图表配色，改善夜间文字对比度及滚动显示。
- 更新顶部账号区、退出按钮与客户端图标，移除订阅列表中重复显示的国旗。
- 页面卡片采用错峰入场动画，并支持系统减少动态效果设置。
- 分配节点弹窗从触发按钮位置展开，加载时预留空间，关闭时保留内容直至动画结束，减少闪动。
- 修正静态确认弹窗的中文语言与主题一致性，统一版本状态标签和重新安装按钮的对齐。

## 修复与调整

- 将 sing-box 新版本检测缓存从 24 小时缩短为 1 小时，缓存到期后打开或刷新节点页面会重新检查，避免新版本发布后长时间没有升级提示。
- 修复面板自更新经 systemd 重启后丢失更新任务标识的问题，使新进程能正确确认更新结果。
- SOCKS 节点仅保留单用户模式，移除多用户入口与按用户生成凭据的逻辑。旧的面板托管 SOCKS 多用户节点自动迁移为持久共享凭据，避免意外变为无认证代理；原有明确配置的无认证节点与原始配置模式不变。
- 编辑外部结构化节点时保留未展示的协议参数；保存 WS / HTTPUpgrade 入站时保留自定义及重复请求头。
- 修复 TLS 模式切换时旧证书材料被错误补回的问题。
- 出站重命名同步更新规则集下载出站引用；删除仍被引用的出站时明确阻止。
- 修复订阅节点名称后缀碰撞，并避开 sing-box 内置出站 tag。
- 导入配置删除入站时同步清理用户授权，不扩大用户访问范围。
- 修复临时流量采样失败后漏计累计增量的问题。
- 更新回滚副本改为停服后生成，保留 SQLite WAL / 回滚日志；补充更新事务互斥与失败重试保护。
- 支持显式重新安装同一版本，以同步重新发布的构建；根据更新任务结果及新进程确认成功，区分失败与回滚。
- 重装时显式指定的新端口或域名正确写入配置，其余配置保持不变。
- sing-box 安装先确认脚本下载成功，再执行并验证实际版本，避免下载失败却显示安装成功。
- 修复旧数据库迁移时用户列表 JSON 序列化错误。

## 升级提示

已经安装原 v1.0.1 的用户可在面板更新区域选择重新安装当前版本，或再次运行下方指定版本的安装命令，以获取本次合并后的界面与修复。

已有 SOCKS 多用户节点升级后改用共享账号密码，请在面板确认节点凭据并刷新客户端订阅；原来的用户专属凭据不再适用。面板更新会同步更新可分发的 Agent 包，已连接服务器上的 Agent 仍需通过面板执行更新。

## 安装 / 更新

```bash
curl -fsSL https://github.com/hann0w0/SingBox-Panel/releases/download/v1.0.1/install.sh | sudo bash -s -- --version v1.0.1
```

采用原生二进制 + systemd 部署。通常重跑安装会保留现有配置与数据。完整说明见 [README](https://github.com/hann0w0/SingBox-Panel/blob/v1.0.1/README.md)。

提供 Linux amd64 / arm64 面板、双架构 Agent 包、前端资源、安装脚本及 SHA-256 校验和。构建产物附带 GitHub Artifact Attestation。
