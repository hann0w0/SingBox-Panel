# Caddy reverse proxy with automatic HTTPS for the panel.
# Caddy handles TLS; the Docker deployment exposes the panel on :32334.
# The agent's reverse WebSocket (/api/agent/ws) is proxied transparently.
panel.example.com {
	reverse_proxy 127.0.0.1:32334
}
